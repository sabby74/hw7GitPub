<!DOCTYPE html>
<html>
<head>
  <title>Gitpub App</title>
  <style>
    body {
      color: blanchedalmond;
      background-color: steelblue;
    }
  </style>
</head>
<body>
  <h1>Foods Menu</h1>
  <h2>Name: <%= food.name.charAt(0).toUpperCase() + food.name.slice(1) %></h2>
  <img src="<%= food.image %>" alt="<%= food.name %>">
  <h3>Price: <%= food.price %></h3>
  <a href="/foods">Back</a>
</body>
</html>
